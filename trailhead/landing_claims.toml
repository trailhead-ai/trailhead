# landing_claims.toml — tracked, typed claims manifest for the Trailhead landing.
#
# Every load-bearing anchor the landing prose names is registered here.
# The link-real gate (test_landing_links_real.py) resolves each [[claim]] against
# the real source of truth and fails closed if any anchor is unresolved or stale.
#
# Schema: array-of-tables [[claim]] with four required fields:
#   kind    — one of: command | skill | agent | doc-link
#             prose-assertion exceptions (not resolved by the gate):
#               positioning | allowlisted-example
#   tool    — trailhead | lore | craft | camp | portage
#   ref     — the anchor to resolve (subcommand name, path, etc.)
#   source  — which README/surface makes this claim: root | lore | craft | camp | portage
#
# The capability-GROUP and install-PRESET concepts were removed: install is now
# config-driven and selects subagents/skills by name. Skill/agent anchors resolve
# against the convention-discovered plugin inventory (Manifest.skills / .subagents).

# ---------------------------------------------------------------------------
# CLI subcommands (kind=command) — the four real commands: install/uninstall/doctor/update
# ---------------------------------------------------------------------------

[[claim]]
kind = "command"
tool = "trailhead"
ref = "install"
source = "root"

[[claim]]
kind = "command"
tool = "trailhead"
ref = "uninstall"
source = "root"

[[claim]]
kind = "command"
tool = "trailhead"
ref = "doctor"
source = "root"

[[claim]]
kind = "command"
tool = "trailhead"
ref = "update"
source = "root"

# ---------------------------------------------------------------------------
# lore skills (kind=skill)
# ---------------------------------------------------------------------------

[[claim]]
kind = "skill"
tool = "lore"
ref = "skills/flush"
source = "lore"

# ---------------------------------------------------------------------------
# lore agents (kind=agent)
# ---------------------------------------------------------------------------

[[claim]]
kind = "agent"
tool = "lore"
ref = "agents/librarian.md"
source = "lore"

# ---------------------------------------------------------------------------
# craft skills (kind=skill)
# ---------------------------------------------------------------------------

[[claim]]
kind = "skill"
tool = "craft"
ref = "skills/consult"
source = "craft"

[[claim]]
kind = "skill"
tool = "craft"
ref = "skills/execute"
source = "craft"

[[claim]]
kind = "skill"
tool = "craft"
ref = "skills/plan"
source = "craft"

# ---------------------------------------------------------------------------
# craft agents (kind=agent)
# ---------------------------------------------------------------------------

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/assumption-prover.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/executor.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/advocate.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/builder.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/breaker.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/attacker.md"
source = "craft"

# ---------------------------------------------------------------------------
# doc-links (kind=doc-link) — real files that the landing references
# ---------------------------------------------------------------------------

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "LICENSE"
source = "root"

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "trailhead/docs/paths.md"
source = "root"

# Root README links to the new config-driven model surfaces (relative doc-links)
[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "config/default.toml"
source = "root"

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "trailhead/harness/base.py"
source = "root"

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "trailhead/harness/__init__.py"
source = "root"

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "trailhead/install_config.py"
source = "root"

# Root README links to the four tool READMEs (relative doc-links)
[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "tools/lore/README.md"
source = "root"

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "tools/craft/README.md"
source = "root"

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "tools/camp/README.md"
source = "root"

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "tools/portage/README.md"
source = "root"

# ---------------------------------------------------------------------------
# lore tool README anchors — skills (kind=skill)
# the per-kind capture skills (area, defer, dead-end, decision, follow-up, check-in, seed) were removed
# ---------------------------------------------------------------------------

[[claim]]
kind = "skill"
tool = "lore"
ref = "skills/search"
source = "lore"

# ---------------------------------------------------------------------------
# craft tool README anchors — skills (kind=skill)
# ---------------------------------------------------------------------------

[[claim]]
kind = "skill"
tool = "craft"
ref = "skills/review"
source = "craft"

# ---------------------------------------------------------------------------
# craft tool README anchors — agents (kind=agent)
# ---------------------------------------------------------------------------

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/planner.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/architect.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/code-reviewer.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/researcher.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/troubleshooter.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/doc-finder.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/test-runner.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/log-sifter.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/security-auditor.md"
source = "craft"

# ---------------------------------------------------------------------------
# portage tool README anchors — skills (kind=skill)
# ---------------------------------------------------------------------------

[[claim]]
kind = "skill"
tool = "portage"
ref = "skills/pull_request"
source = "portage"

# ---------------------------------------------------------------------------
# portage tool README anchors — agents (kind=agent)
# ---------------------------------------------------------------------------

[[claim]]
kind = "agent"
tool = "portage"
ref = "agents/updater.md"
source = "portage"

[[claim]]
kind = "agent"
tool = "portage"
ref = "agents/monitor.md"
source = "portage"

[[claim]]
kind = "agent"
tool = "portage"
ref = "agents/summarizer.md"
source = "portage"
