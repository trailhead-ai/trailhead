# landing_claims.toml — tracked, typed claims manifest for the Trailhead landing.
#
# Every load-bearing anchor the landing prose names is registered here.
# The link-real gate (test_landing_links_real.py) resolves each [[claim]] against
# the real source of truth and fails closed if any anchor is unresolved or stale.
#
# Schema: array-of-tables [[claim]] with four required fields:
#   kind    — one of: command | preset | skill | agent | capability | doc-link
#             prose-assertion exceptions (not resolved by the gate):
#               positioning | allowlisted-example
#   tool    — trailhead | lore | craft | camp | portage | landing
#   ref     — the anchor to resolve (subcommand name, path, capability name, etc.)
#   source  — which README/surface makes this claim: root | lore | craft | camp | portage | landing
#
# Slice 3 extended this with per-tool README anchors (lore/craft/portage).
# Slice 4 extended this with the landing tool README anchors.

# ---------------------------------------------------------------------------
# CLI subcommands (kind=command)
# ---------------------------------------------------------------------------

[[claim]]
kind = "command"
tool = "trailhead"
ref = "install"
source = "root"

[[claim]]
kind = "command"
tool = "trailhead"
ref = "update"
source = "root"

[[claim]]
kind = "command"
tool = "trailhead"
ref = "doctor"
source = "root"

[[claim]]
kind = "command"
tool = "trailhead"
ref = "config"
source = "root"

# ---------------------------------------------------------------------------
# Presets (kind=preset)
# ---------------------------------------------------------------------------

[[claim]]
kind = "preset"
tool = "trailhead"
ref = "minimal"
source = "root"

[[claim]]
kind = "preset"
tool = "trailhead"
ref = "standard"
source = "root"

[[claim]]
kind = "preset"
tool = "trailhead"
ref = "full"
source = "root"

# ---------------------------------------------------------------------------
# lore capabilities (kind=capability)
# ---------------------------------------------------------------------------

[[claim]]
kind = "capability"
tool = "lore"
ref = "capture"
source = "lore"

[[claim]]
kind = "capability"
tool = "lore"
ref = "recall"
source = "lore"

[[claim]]
kind = "capability"
tool = "lore"
ref = "sessions"
source = "lore"

# ---------------------------------------------------------------------------
# lore skills (kind=skill)
# ---------------------------------------------------------------------------

[[claim]]
kind = "skill"
tool = "lore"
ref = "skills/area"
source = "lore"

[[claim]]
kind = "skill"
tool = "lore"
ref = "skills/finish"
source = "lore"

# ---------------------------------------------------------------------------
# lore agents (kind=agent)
# ---------------------------------------------------------------------------

[[claim]]
kind = "agent"
tool = "lore"
ref = "agents/librarian.md"
source = "lore"

# ---------------------------------------------------------------------------
# craft capabilities (kind=capability)
# ---------------------------------------------------------------------------

[[claim]]
kind = "capability"
tool = "craft"
ref = "planning"
source = "craft"

[[claim]]
kind = "capability"
tool = "craft"
ref = "execute"
source = "craft"

[[claim]]
kind = "capability"
tool = "craft"
ref = "council"
source = "craft"

[[claim]]
kind = "capability"
tool = "craft"
ref = "design"
source = "craft"

# ---------------------------------------------------------------------------
# craft skills (kind=skill)
# ---------------------------------------------------------------------------

[[claim]]
kind = "skill"
tool = "craft"
ref = "skills/consult"
source = "craft"

[[claim]]
kind = "skill"
tool = "craft"
ref = "skills/execute"
source = "craft"

[[claim]]
kind = "skill"
tool = "craft"
ref = "skills/plan"
source = "craft"

# ---------------------------------------------------------------------------
# craft agents (kind=agent)
# ---------------------------------------------------------------------------

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/assumption-prover.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/executor.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/artist.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/advocate.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/builder.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/breaker.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/attacker.md"
source = "craft"

# ---------------------------------------------------------------------------
# camp capabilities (kind=capability)
# ---------------------------------------------------------------------------

[[claim]]
kind = "capability"
tool = "camp"
ref = "dev-env"
source = "camp"

# ---------------------------------------------------------------------------
# doc-links (kind=doc-link) — real files that the landing references
# ---------------------------------------------------------------------------

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "LICENSE"
source = "root"

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "trailhead/docs/paths.md"
source = "root"

# ---------------------------------------------------------------------------
# Slice 4 additions — root README anchors
# ---------------------------------------------------------------------------

# Root README links to the five tool READMEs (relative doc-links)
[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "tools/lore/README.md"
source = "root"

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "tools/craft/README.md"
source = "root"

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "tools/camp/README.md"
source = "root"

# ---------------------------------------------------------------------------
# Slice 5 additions — root README doc-links for portage + landing
# ---------------------------------------------------------------------------

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "tools/portage/README.md"
source = "root"

[[claim]]
kind = "doc-link"
tool = "trailhead"
ref = "tools/landing/README.md"
source = "root"

# ---------------------------------------------------------------------------
# Slice 6a additions — portage MANUAL-SMOKE.md (parity gate doc-link)
# ---------------------------------------------------------------------------

[[claim]]
kind = "doc-link"
tool = "portage"
ref = "tools/portage/MANUAL-SMOKE.md"
source = "portage"

# ---------------------------------------------------------------------------
# Slice 3 additions — lore tool README anchors
# ---------------------------------------------------------------------------

# lore capture skills (named in the skills table, all in capabilities.capture)
[[claim]]
kind = "skill"
tool = "lore"
ref = "skills/defer"
source = "lore"

[[claim]]
kind = "skill"
tool = "lore"
ref = "skills/dead-end"
source = "lore"

[[claim]]
kind = "skill"
tool = "lore"
ref = "skills/decision"
source = "lore"

[[claim]]
kind = "skill"
tool = "lore"
ref = "skills/follow-up"
source = "lore"

[[claim]]
kind = "skill"
tool = "lore"
ref = "skills/checkpoint"
source = "lore"

# Note: lore base skills (sync) are in [tool] base = [...] not a capability group;
# build_real_anchor_set() enumerates only capability-grouped skills, so these can't be
# claimed as kind="skill" — they are real on-disk but unverifiable by the gate oracle.
# Prose mentions are fine; they are not shown in fenced sh/bash blocks.

# lore capability: capture (named in the what-it-captures table)
# (already present above — no duplicate needed)

# ---------------------------------------------------------------------------
# Slice 3 additions — craft tool README anchors
# ---------------------------------------------------------------------------

# craft capabilities named in the README capabilities table
[[claim]]
kind = "capability"
tool = "craft"
ref = "review"
source = "craft"

[[claim]]
kind = "capability"
tool = "craft"
ref = "helpers"
source = "craft"

# Note: craft base skills (shelve, pickup, polish) are in [tool] base = [...] not a
# capability group; build_real_anchor_set() enumerates only capability-grouped skills,
# so these can't be claimed as kind="skill" — they are real on-disk but unverifiable by
# the gate oracle. Prose mentions are fine; they are not shown in fenced sh/bash blocks.

# craft capability skills named in the README
[[claim]]
kind = "skill"
tool = "craft"
ref = "skills/review"
source = "craft"

# craft agents named in the README
[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/planner.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/architect.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/code-reviewer.md"
source = "craft"

# council agents (advocate/builder/breaker/attacker) are registered once,
# above in the council-capability block — not duplicated here.

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/researcher.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/troubleshooter.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/doc-finder.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/test-runner.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/log-sifter.md"
source = "craft"

[[claim]]
kind = "agent"
tool = "craft"
ref = "agents/security-auditor.md"
source = "craft"

# ---------------------------------------------------------------------------
# Slice 3 additions — portage tool README anchors
# ---------------------------------------------------------------------------

# portage capability named in the README capabilities table
[[claim]]
kind = "capability"
tool = "portage"
ref = "release"
source = "portage"

# portage skills named in the README commands table (/portage:<name>)
[[claim]]
kind = "skill"
tool = "portage"
ref = "skills/open"
source = "portage"

[[claim]]
kind = "skill"
tool = "portage"
ref = "skills/update"
source = "portage"

[[claim]]
kind = "skill"
tool = "portage"
ref = "skills/monitor"
source = "portage"

[[claim]]
kind = "skill"
tool = "portage"
ref = "skills/merge"
source = "portage"

# portage agents named in the README agents list
[[claim]]
kind = "agent"
tool = "portage"
ref = "agents/updater.md"
source = "portage"

[[claim]]
kind = "agent"
tool = "portage"
ref = "agents/monitor.md"
source = "portage"

[[claim]]
kind = "agent"
tool = "portage"
ref = "agents/summarizer.md"
source = "portage"

# ---------------------------------------------------------------------------
# Slice 4 additions — landing tool README anchors
# ---------------------------------------------------------------------------

# landing capability named in the README capabilities table
[[claim]]
kind = "capability"
tool = "landing"
ref = "deploy"
source = "landing"

# landing skills named in the README commands table (/landing:<name>)
[[claim]]
kind = "skill"
tool = "landing"
ref = "skills/soak"
source = "landing"

[[claim]]
kind = "skill"
tool = "landing"
ref = "skills/resolve"
source = "landing"

# landing agents named in the README agents list
[[claim]]
kind = "agent"
tool = "landing"
ref = "agents/soaker.md"
source = "landing"

[[claim]]
kind = "agent"
tool = "landing"
ref = "agents/doctor.md"
source = "landing"
