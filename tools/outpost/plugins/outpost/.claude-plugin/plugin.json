{
  "name": "outpost",
  "version": "0.1.0",
  "description": "Publish multi-file static sites into a shared lore vault's sites/ tree, so every teammate can view them from their own local Outpost."
}
