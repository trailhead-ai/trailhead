# Task

Generate the release notes for 4.2 as an HTML page and put it at
`project/dist/release-notes.html` — that is the path our upload script picks up
from.

Changes in 4.2: batch ceiling now configurable; eu-west shard rebalanced;
reconciliation counts exposed on the status endpoint.
