# outpost — the human→agent review loop over the local outpost cockpit daemon.
# tool.name MUST equal the plugins/<name>/ dir ("outpost").
# Skill-only: no python package, no subagents, no always-on base. The one skill
# (skills/pickup-review/) is a pure-HTTP contract, discovered on disk by name.

[tool]
name = "outpost"
