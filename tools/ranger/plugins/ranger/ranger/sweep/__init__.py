"""The sweep domain: queue derivation, the per-vault lock, and the exit report.

Deliberately empty for now — the modules land alongside their own tests. The
package exists so the CLI's imports have a stable target and so the sweep's
domain code never leaks into ``ranger.cli``.
"""
