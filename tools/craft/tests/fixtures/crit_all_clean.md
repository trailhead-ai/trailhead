# Fixture: all-clean spec

Synthetic fixture spec body for `criterion_gate.py` tests.

## Acceptance Criteria

- **AC1.** A clean criterion naming a slash-command like `/craft:slice`.
  *Verified by: automated assertion.*
- **AC2.** A clean criterion naming a CLI flag like `--related`.
  *Verified by: design doc review.*
- **AC3.** A clean criterion naming a record field label like `**Covers:**`.
  *Verified by: manual check.*

## Non-Goals

n/a — fixture only.
