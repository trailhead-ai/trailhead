# Fixture: one criterion per refused code-location form

Synthetic fixture spec body for `criterion_gate.py` tests.

## Acceptance Criteria

- **AC1.** A criterion naming a path with an extension like
  `tools/craft/scripts/criterion_gate.py`. *Verified by: automated assertion.*
- **AC2.** A criterion naming a path:line reference like
  `covers_gate.py:191`. *Verified by: automated assertion.*
- **AC3.** A criterion naming call syntax like `parse_criteria()`.
  *Verified by: automated assertion.*
- **AC4.** A criterion naming an HTTP verb plus path like `GET /api/foo`.
  *Verified by: automated assertion.*
- **AC5.** A criterion naming a symbol like `event_id`.
  *Verified by: automated assertion.*

## Non-Goals

n/a — fixture only.
