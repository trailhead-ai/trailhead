# Fixture: duplicate ledger task id

Synthetic fixture spec body for `ledger_gate.py` tests — not a real vault record.

## Acceptance Criteria

- **AC1.** A fixture criterion.
- **AC2.** A fixture criterion.

## Slices

- **First slice** — a fixture value claim. (`task/dup`, closed 2026-01-01, covers AC1)
- **Second slice** — a fixture value claim, a rewrite that did not replace. (`task/dup`, closed 2026-01-02, covers AC2)
