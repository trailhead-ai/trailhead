# Fixture: a full entry, then a later partial entry, over the same identifier

Synthetic fixture spec body for `candidate_set.py` tests — not a real vault record. The
opposite ledger order from `spec_candidate_partial_then_full.md`, pinning that the union is
order-free — full still wins.

## Acceptance Criteria

- **AC1.** A fixture criterion.
- **AC2.** A fixture criterion.
- **AC3.** A fixture criterion.
- **AC4.** A fixture criterion.
- **AC5.** A fixture criterion.
- **AC6.** A fixture criterion.
- **AC7.** A fixture criterion.
- **AC8.** A fixture criterion.
- **AC9.** A fixture criterion.

## Slices

- **First slice** — a fixture value claim. (`task/first`, closed 2026-01-01, covers AC2)
- **Second slice** — a fixture value claim. (`task/second`, closed 2026-01-02, partially covers AC2)
