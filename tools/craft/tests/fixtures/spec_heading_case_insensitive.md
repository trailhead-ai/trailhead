# Fixture: spec whose Acceptance Criteria heading uses lowercase casing

Synthetic fixture spec body for `covers_gate.py` tests — not a real vault record.
Mirrors specs in this vault (e.g. spec/outpost-work-stream-store-and-agent-facing-api)
that write the heading as `## Acceptance criteria` (lowercase `c`) rather than the
exact-case `## Acceptance Criteria` the parser must not require.

## Problem

A fixture problem statement.

## Acceptance criteria

- **AC1.** A real fixture criterion, atomic and observable.
- **AC2.** Another real fixture criterion.

## Non-Goals

n/a — fixture only.
