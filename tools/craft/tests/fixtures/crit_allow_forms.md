# Fixture: one criterion per allowed product-surface form

Synthetic fixture spec body for `criterion_gate.py` tests.

## Acceptance Criteria

- **AC1.** A criterion naming a slash-command like `/craft:slice`.
  *Verified by: automated assertion.*
- **AC2.** A criterion naming a CLI flag like `--related`.
  *Verified by: automated assertion.*
- **AC3.** A criterion naming a CLI subcommand like `lore areas`.
  *Verified by: automated assertion.*
- **AC4.** A criterion naming a record field label like `**Covers:**`.
  *Verified by: automated assertion.*
- **AC5.** A criterion naming a section heading like `## Slices`.
  *Verified by: automated assertion.*

## Non-Goals

n/a — fixture only.
