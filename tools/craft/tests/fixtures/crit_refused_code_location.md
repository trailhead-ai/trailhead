# Fixture: one refused code-location span

Synthetic fixture spec body for `criterion_gate.py` tests.

## Acceptance Criteria

- **AC1.** A clean criterion. *Verified by: automated assertion.*
- **AC2.** A criterion naming a bare field like `event_id`.
  *Verified by: automated assertion.*

## Non-Goals

n/a — fixture only.
