# Fixture: unidentified bullet whose text is credential-shaped

Synthetic fixture spec body for `criterion_gate.py` tests.

## Acceptance Criteria

- **AC1.** A clean criterion. *Verified by: automated assertion.*
- password=hunter2supersecretvalue leaks in this unidentified bullet.

## Non-Goals

n/a — fixture only.
