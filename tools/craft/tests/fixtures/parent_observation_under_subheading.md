**Value claim:** A `### ` sub-heading nested inside `## Criterion observations` does not
close the section — a deliberate choice, pinned here so it cannot silently invert. The
observation bullet grouped under the sub-heading still counts.

**Covers:** AC9

## Criterion observations

### Detail

- **AC9** — automated-assertion — tools/craft/tests/test_observation_gate.py::test_ac9
