# Fixture: spec body with no Acceptance Criteria heading

Synthetic fixture spec body for `covers_gate.py` tests — not a real vault record.

## Problem

A fixture problem statement.

## Objectives

- This fixture deliberately omits the `## Acceptance Criteria` heading, to
  exercise the fail-closed path.

Note the drafting guidance below mentions `## Acceptance Criteria` inline,
mid-paragraph, as a loose prose reference — this must NOT satisfy the gate's
heading anchor, which requires the heading at line start.

## Non-Goals

n/a — fixture only.
