# Fixture: nine-criterion spec body

Synthetic fixture spec body for `covers_gate.py` tests — not a real vault record.

## Problem

A fixture problem statement, long enough to look like prose but carrying no
real content.

## Objectives

- Exercise the covers gate against a spec declaring AC1 through AC9.

## Acceptance Criteria

- **AC1.** A fixture criterion, atomic and observable.
- **AC2.** Another fixture criterion.

### A grouping sub-heading

The sub-heading above groups criteria without itself being one — it must not
be parsed as a criterion identifier.

- **AC3.** A fixture criterion under the grouping sub-heading.
- **AC4.** A fixture criterion that carries a nested qualifier.
  - A nested sub-bullet that qualifies AC4 rather than forming its own
    criterion. This bullet is indented and must not be counted.
- **AC5.** A fixture criterion.
- **AC6.** A fixture criterion.
- **AC7.** A fixture criterion.
- **AC8.** A fixture criterion.
- **AC9.** A fixture criterion.

## Required Interfaces

n/a — fixture only.

## Non-Goals

n/a — fixture only.

## Constraints

n/a — fixture only.

## UI Direction

n/a — fixture only.

## Open Questions / Risks

n/a — fixture only.

## Related

n/a — fixture only.
