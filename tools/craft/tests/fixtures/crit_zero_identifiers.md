# Fixture: zero-identifier spec (legacy carve-out)

Synthetic fixture spec body for `criterion_gate.py` tests.

## Acceptance Criteria

- A plain bullet with no identifier prefix.
- Another plain bullet.

## Non-Goals

n/a — fixture only.
