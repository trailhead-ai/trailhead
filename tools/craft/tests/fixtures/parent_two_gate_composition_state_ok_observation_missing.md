**Value claim:** Composition fixture — this parent carries no `## Enumerated states` section
at all, so the state-coverage gate would pass trivially. The observation gate must still
refuse this parent on its own missing observation for AC9.

**Covers:** AC9
