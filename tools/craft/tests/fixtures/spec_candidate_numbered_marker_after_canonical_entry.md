# Fixture: a numbered-marker ledger line after the last canonical entry

Synthetic fixture spec body for `candidate_set.py` tests — not a real vault record. The
second ledger line below predates the canonical top-level `- ` bullet convention — marked
with a numbered-list marker instead — and sits after the canonical entry that precedes it.

## Acceptance Criteria

- **AC1.** A fixture criterion.
- **AC2.** A fixture criterion.
- **AC3.** A fixture criterion.
- **AC4.** A fixture criterion.
- **AC5.** A fixture criterion.
- **AC6.** A fixture criterion.
- **AC7.** A fixture criterion.
- **AC8.** A fixture criterion.
- **AC9.** A fixture criterion.

## Slices

- **New slice** — a fixture value claim covering every declared criterion. (`task/new`, closed 2025-12-15, covers AC1, AC2, AC3, AC4, AC5, AC6, AC7, AC8, AC9)
1. **Old slice** — a legacy entry using a numbered marker predating the ledger convention. (`task/old`, closed 2025-12-01)
