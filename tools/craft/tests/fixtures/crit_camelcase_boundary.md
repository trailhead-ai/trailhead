# Fixture: CamelCase boundary — genuine alternation vs all-caps token

Synthetic fixture spec body for `criterion_gate.py` tests.

## Acceptance Criteria

- **AC1.** A criterion naming a genuine mixed-case type like `JobRuntime`.
  *Verified by: automated assertion.*
- **AC2.** A criterion naming an all-caps closed-vocabulary token like
  `DELETE`. *Verified by: automated assertion.*

## Non-Goals

n/a — fixture only.
