**Value claim:** A manual-check observation with an `## Operator attestations` line for the
same identifier, but the attestation text is whitespace only — the guard must treat that
the same as no attestation at all.

**Covers:** AC3

## Criterion observations

- **AC3** — manual-check — checked at close by the operator: verified the button renders

## Operator attestations

- **AC3** —    
