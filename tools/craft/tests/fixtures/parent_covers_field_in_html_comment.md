**Value claim:** A second `**Covers:**` line is masked inside an HTML comment — the
claim it makes is larger than the first, and only the first's identifier has a recorded
observation. The comment-masked duplicate must be as invisible as the fence-masked one is.

**Covers:** AC1

<!--
**Covers:** AC1, AC2
-->

## Criterion observations

- **AC1** — automated-assertion — tools/craft/tests/test_observation_gate.py::test_ac1
