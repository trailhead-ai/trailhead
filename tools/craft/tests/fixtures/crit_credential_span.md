# Fixture: refused span carrying a credential-shaped secret

Synthetic fixture spec body for `criterion_gate.py` tests.

## Acceptance Criteria

- **AC1.** A criterion calling
  `configure(api_key="sk_live_Zq7Kd2")`.
  *Verified by: automated assertion.*

## Non-Goals

n/a — fixture only.
