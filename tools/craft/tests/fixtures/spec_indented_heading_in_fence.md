# Fixture: indented heading inside a fenced example

Synthetic fixture spec body for `covers_gate.py` tests — not a real vault record.

## Problem

A fixture problem statement, long enough to look like prose but carrying no
real content.

## Objectives

- Exercise the heading anchor against a fenced example that contains an
  indented `## Acceptance Criteria` heading above the real section.

Here is a worked example of the shape a spec's acceptance criteria take:

```markdown
  ## Acceptance Criteria

  - **AC1.** A fake criterion inside the worked example, indented under the
    fence — must not be treated as the real heading.
```

## Acceptance Criteria

- **AC1.** A real fixture criterion, atomic and observable.
- **AC2.** Another real fixture criterion.

## Non-Goals

n/a — fixture only.
