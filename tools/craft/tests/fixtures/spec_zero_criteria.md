# Fixture: spec with an Acceptance Criteria heading but no identifiers

Synthetic fixture spec body for `covers_gate.py` tests — not a real vault record.
Predates the `**ACn.**` convention: the heading is present but declares no
criterion identifiers, the legacy shape this gate must fail closed on.

## Problem

A fixture problem statement.

## Acceptance Criteria

- A reviewer can approve a submission, and the submitter is notified.
- A manager can change a shift's start and end times, with validation
  against opening hours.

## Non-Goals

n/a — fixture only.
