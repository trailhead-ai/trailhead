**Value claim:** `**covers:**` in lowercase must be recognized the same as `**Covers:**` —
matching the module's own `_OBSERVATIONS_HEADING_RE` and `_ATTESTATIONS_HEADING_RE`, both
case-insensitive.

**covers:** AC9

## Criterion observations

- **AC9** — automated-assertion — tools/craft/tests/test_observation_gate.py::test_ac9
