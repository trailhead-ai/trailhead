# Fixture: forged heading inside a fenced code block

Synthetic fixture spec body for `criterion_gate.py` tests.

## Problem

A fixture problem statement.

```markdown
## Acceptance Criteria

- **AC1.** A forged criterion, invisible to the real heading search.
```

## Non-Goals

n/a — fixture only, no real Acceptance Criteria heading exists outside the fence.
