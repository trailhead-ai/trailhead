**Value claim:** Only `**Partially covers:**` is present — no `**Covers:**` field, and no
observation for the identifier it names. The gate reads `**Covers:**` only, never
`**Partially covers:**`, so it must not refuse on that identifier's account.

**Partially covers:** AC9
