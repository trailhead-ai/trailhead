# Fixture: a line's covers token widened in place beyond its parent

Synthetic fixture spec body for `ledger_gate.py` tests — not a real vault record.
The council's deliberate-edit case: the parent record still reads `covers AC2`,
but this ledger line was widened in place to `covers AC2, AC5` after being
appended.

## Acceptance Criteria

- **AC1.** A fixture criterion.
- **AC2.** A fixture criterion.
- **AC5.** A fixture criterion.

## Slices

- **First slice** — a fixture value claim. (`task/alpha`, closed 2026-01-01, covers AC2, AC5)
