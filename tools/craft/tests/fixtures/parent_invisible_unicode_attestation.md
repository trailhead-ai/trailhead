**Value claim:** A manual-check observation whose `## Operator attestations` line is a
single U+200B ZERO WIDTH SPACE — invisible in any editor, but not `str.isspace()`, so a
plain `.strip()` guard treats it as a real attestation.

**Covers:** AC3

## Criterion observations

- **AC3** — manual-check — checked at close by the operator: verified the button renders

## Operator attestations

- **AC3** — ​
