# Fixture: an implementation identifier hidden in a sub-bullet, separated by
# a blank line (loose-list form of crit_subbullet_identifier_leak.md)

Synthetic fixture spec body for `criterion_gate.py` tests.

## Acceptance Criteria

- **AC1.** A criterion that qualifies below. *Verified by: automated assertion.*

  - See `covers_gate.py:191` for the exact site.

## Non-Goals

n/a — fixture only.
