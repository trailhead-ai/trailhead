# Fixture: three distinct violations across three criteria

Synthetic fixture spec body for `criterion_gate.py` tests.

## Acceptance Criteria

- **AC1.** A clean criterion. *Verified by: automated assertion.*
- **AC2.** A criterion naming a bare symbol like `event_id`.
  *Verified by: automated assertion.*
- **AC3.** A criterion with no trailer at all.
- **AC4.** A criterion with a bad trailer. *Verified by: manual testing.*

## Non-Goals

n/a — fixture only.
