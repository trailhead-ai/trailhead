Review passed and the change is merged — [spec/dock-scheduling-windows](http://127.0.0.1:7313/records/harborlight/spec/dock-scheduling-windows) carries `craft/slice-loop=complete`, so the slice loop reports this spec closed out. There's no next slice to choose. Run this when you're ready to distill this work into the ADR log.

```
/craft:distill spec/dock-scheduling-windows
```
