The plan is written to [task/the-ledger-reconciliation-slice](http://127.0.0.1:7313/records/causewell/task/the-ledger-reconciliation-slice) — the slice parent now carries the full plan body (goal, delta design, given axioms, known unknowns, flow-out gate), with the component-shaped child tasks wired beneath it in dependency order.

Council Review ran all four lenses. One Critical — a missing idempotency check on the reconciliation write — was raised and is dispositioned `resolved` in the plan's `## Council Review` section. No Critical is undisposed.

Reply **build** to hand off to `/craft:execute` and start building task by task, or run this in a fresh session. Call out anything that needs adjustment first.

```
/craft:execute task/the-ledger-reconciliation-slice
```
