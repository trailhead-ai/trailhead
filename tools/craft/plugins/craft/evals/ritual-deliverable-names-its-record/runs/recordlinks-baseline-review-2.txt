Review came back `SHIP` with zero Critical findings, and the change is merged.

The slice loop reports [spec/dock-scheduling-windows](http://127.0.0.1:7313/records/harborlight/spec/dock-scheduling-windows) closed out — `craft/slice-loop=complete`, so there is no next slice to choose. Run this when you're ready to distill this work into the ADR log.

```
/craft:distill spec/dock-scheduling-windows
```
