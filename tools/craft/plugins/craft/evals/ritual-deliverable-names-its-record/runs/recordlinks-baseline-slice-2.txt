**Slice chosen: The berth allocation slice** — the smallest next step above the value floor within the current phase of [spec/dock-scheduling](http://127.0.0.1:7313/records/harborlight/spec/dock-scheduling).

**Value claim:** lets a dock operator assign an inbound vessel to a specific berth and see the assignment reflected on the schedule board, closing the gap between "vessel is expected" and "vessel has a berth."

It makes **AC3** fully green, and makes no acceptance criterion only partially green. It touches no visual surface beyond the schedule board update an earlier slice already covered, so it carries no enumerated states.

The parent task is [task/the-berth-allocation-slice](http://127.0.0.1:7313/records/harborlight/task/the-berth-allocation-slice), written at `in-progress` and linked to the spec. Read it back with:

```
lore record show task/the-berth-allocation-slice
```

Run this next to design the slice's internals:

```
/craft:plan task/the-berth-allocation-slice
```
