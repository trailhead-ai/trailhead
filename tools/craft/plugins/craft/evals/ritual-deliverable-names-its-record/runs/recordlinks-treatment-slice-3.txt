**Chosen slice: "The berth allocation slice"** — materialized as [task/the-berth-allocation-slice](http://127.0.0.1:7313/records/harborlight/task/the-berth-allocation-slice), status `in-progress`, linked to [spec/dock-scheduling](http://127.0.0.1:7313/records/harborlight/spec/dock-scheduling).

**Value claim:** lets a dock operator assign an inbound vessel to a specific berth and see the assignment reflected on the schedule board, closing the gap between "vessel is expected" and "vessel has a berth."

**Covers:** AC3 fully. No acceptance criteria are made only partially green. The slice touches no visual surface beyond the schedule board update an earlier slice already covers, so the parent carries no `## Enumerated states` section.

Read the parent back with:

```
lore record show task/the-berth-allocation-slice
```

Run this next to design the slice:

```
/craft:plan task/the-berth-allocation-slice
```
