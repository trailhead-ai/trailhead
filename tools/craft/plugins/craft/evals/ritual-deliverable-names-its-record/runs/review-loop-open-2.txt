Review passed and the change is merged. This closes the review's own job, not the lifecycle of [spec/dock-scheduling-windows](http://127.0.0.1:7313/records/harborlight/spec/dock-scheduling-windows) — the slice loop hasn't reported that spec closed out, so further slices remain to be chosen, planned, or built.

Run `/craft:slice spec/dock-scheduling-windows` to choose the next slice.
