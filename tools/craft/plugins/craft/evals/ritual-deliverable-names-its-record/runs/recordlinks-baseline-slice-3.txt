**Chosen slice: "The berth allocation slice"** — the smallest next step above the value floor within the current phase of [spec/dock-scheduling](http://127.0.0.1:7313/records/harborlight/spec/dock-scheduling).

**Value claim:** it lets a dock operator assign an inbound vessel to a specific berth and see that assignment reflected on the schedule board — closing the gap between "vessel is expected" and "vessel has a berth."

This slice touches no visual surface beyond the schedule board update an earlier slice already covers, so it carries no `## Enumerated states` section. It makes **AC3** fully green, and makes no acceptance criterion only partially green.

**Parent task:** [task/the-berth-allocation-slice](http://127.0.0.1:7313/records/harborlight/task/the-berth-allocation-slice), written at `in-progress` and linked to the spec. Read it back with:

```
lore record show task/the-berth-allocation-slice
```

Run this next to design the slice's internals:

```
/craft:plan task/the-berth-allocation-slice
```
