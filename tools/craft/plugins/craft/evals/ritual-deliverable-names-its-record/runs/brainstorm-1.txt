The Exit Gate checklist is green across the board:

- [x] Shared understanding confirmed (step 5) — you agreed to a playback of exactly what gets built
- [x] Objectives clear and outcome-framed
- [x] Acceptance criteria testable and bounded
- [x] Non-goals explicit
- [x] Open questions all resolved, deferred, or accepted-as-risk — none unaddressed
- [x] UI direction locked — the handheld scanner's "slot claimed" state, described verbally in the spec
- [x] Exit artifact written and shared — the one spec
- [x] No scope was deferred, so there are no deferred `task` records to carry forward

The spec is saved as a lore `spec` record, [spec/warehouse-picking-batches](http://127.0.0.1:7313/records/northlight/spec/warehouse-picking-batches) (status `draft`). Next it goes through the gauntlet — eight parallel passes that attack its facts, premises, consistency, and underdetermination — and the gauntlet flips it to `ready` once you've accepted its recommendation, or overridden it. Run:

```
/craft:gauntlet spec/warehouse-picking-batches
```

Invoke that yourself in a fresh session so the skill loads cleanly — I won't enter it from here. Once the gauntlet has flipped the spec to `ready`, the slice loop picks it up:

```
/craft:slice spec/warehouse-picking-batches
```
