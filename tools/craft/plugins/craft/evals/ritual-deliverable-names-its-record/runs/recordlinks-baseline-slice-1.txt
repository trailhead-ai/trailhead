Chosen slice: **The berth allocation slice** — [task/the-berth-allocation-slice](http://127.0.0.1:7313/records/harborlight/task/the-berth-allocation-slice), written at `in-progress` and linked to [spec/dock-scheduling](http://127.0.0.1:7313/records/harborlight/spec/dock-scheduling).

**Value claim:** lets a dock operator assign an inbound vessel to a specific berth and see the assignment reflected on the schedule board, closing the gap between "vessel is expected" and "vessel has a berth."

It covers AC3 fully and no acceptance criterion partially. It touches no visual surface beyond the schedule board update an earlier slice already covered, so the parent carries no `## Enumerated states` section.

To see the record as written:

```
lore record show task/the-berth-allocation-slice
```

Next, design the slice:

```
/craft:plan task/the-berth-allocation-slice
```
