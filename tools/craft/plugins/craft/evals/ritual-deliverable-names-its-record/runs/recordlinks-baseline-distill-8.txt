**Cluster: `spec/dock-scheduling-windows`** (vault `harborlight`) — single member, no siblings.

| | |
|---|---|
| Members | `spec/dock-scheduling-windows` → `complete`, annotated `distilled=adr` |
| ADRs written | `adr/dock-scheduling-windows-use-fifo-slots` (vault `harborlight`), `related: spec=dock-scheduling-windows` |
| Decisions absorbed | none |
| ADRs superseded | none |
| Area profiles re-synthesized | the areas this spec touched, citing the new ADR inline |
| Terminal outcome | **ADRs written** |

No deferrals: no in-flight members, and no spec hit the zero-task-edges case. This was the only cluster in the run.

Read the ADR:

```
lore record show adr/dock-scheduling-windows-use-fifo-slots
```
