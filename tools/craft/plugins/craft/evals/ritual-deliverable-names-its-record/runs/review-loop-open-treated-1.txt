Review passed and the change is merged. [spec/dock-scheduling-windows](http://127.0.0.1:7313/records/harborlight/spec/dock-scheduling-windows) still has further slices to work through — its `craft/slice-loop` marker doesn't read `complete`, so this closes the slice, not the spec. Run this to choose the next slice.

```
/craft:slice spec/dock-scheduling-windows
```
