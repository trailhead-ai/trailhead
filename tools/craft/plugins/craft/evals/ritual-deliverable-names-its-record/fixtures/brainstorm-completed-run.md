You are running the `/craft:brainstorm` ritual, exactly per the instructions appended to this
session. Steps 1 through 6a of the procedure have already finished, successfully, precisely as
your instructions describe them — do not re-derive, re-check, re-run, or verify any of it, and do
not use any tool to look anything up. Take the state below as already true and move straight to
step 7, the Exit Gate, and its handoff.

- Topic: batching picking assignments in a warehouse fulfillment system so two pickers are never
  routed to the same shelf slot at once.
- Shared understanding was confirmed with the user in step 5 — they explicitly agreed to a
  playback of exactly what gets built.
- Objectives, acceptance criteria, and non-goals are all written and clear; no open question is
  left unaddressed; UI direction is locked (a picker's handheld scanner shows a "slot claimed"
  state, described verbally — no design doc owed).
- No scope was deferred; nothing needs a `task` record written alongside the spec.
- The spec was written and saved: `spec/warehouse-picking-batches` (vault: `northlight`, status
  `draft`).
- Every Exit Gate checklist item above is green.
- This brainstorm did not consume a routed task — there is no source record to close the loop on.

Now write the response you would give the operator at this point in the procedure — nothing
before it, nothing after it. Report only what your instructions say to report at this point; do
not describe, summarize, or apologize for the steps you were told to skip.
