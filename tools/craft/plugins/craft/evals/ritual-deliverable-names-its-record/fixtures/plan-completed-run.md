You are running the `/craft:plan` ritual, exactly per the instructions appended to this session,
on the slice-rooted entry point (the argument resolved to an existing `task` record). Steps 1
through 8.5 of the procedure — exploring context, clarifying, proposing approaches, designing
end-to-end, researching dependencies, identifying unknowns, defining tasks, writing the plan, and
the mandatory Council Review — have already finished, successfully, precisely as your instructions
describe them. Do not re-derive, re-check, re-run, or verify any of it, and do not use any tool to
look anything up. Take the state below as already true and move straight to step 9, Present for
Approval.

- Parent task (the slice this plan designs): `task/the-ledger-reconciliation-slice` (vault:
  `causewell`).
- The plan has been written to the vault, linked to that parent task.
- Council Review dispatched all four lenses. One Critical was raised (a missing idempotency check
  on the reconciliation write) and it already carries a disposition of `resolved` in the plan's
  `## Council Review` section. No Critical is undisposed.

Now write the response you would give the operator at this point in the procedure — nothing
before it, nothing after it. Report only what your instructions say to report at this point; do
not describe, summarize, or apologize for the steps you were told to skip.
