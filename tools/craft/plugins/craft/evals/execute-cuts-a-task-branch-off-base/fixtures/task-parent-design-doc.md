# task/fixture-parent-with-design-doc

**status:** in-progress

**Children:**
- task/fixture-child-a (ready)
- task/fixture-child-b (ready)

**Labels:** craft/design-doc=docs/design/fixture-slice.md

## Problem

The fixture slice needs a design doc written up front covering its enumerated states before its
two child tasks are decomposed further.

## Enumerated states

- state-a
- state-b

This is the parent task record `### Workspace preflight`'s design-doc check reads: it has children
(the parent-with-children shape), and its `craft/design-doc` label names
`docs/design/fixture-slice.md`, relative to the fixture repo's working directory.
