"""The ``lore`` package — lore's own domain modules, organized by subpackage.

Imported by ``cli/lore`` (and the test suite) as a real package so internal
modules can use relative imports instead of ``sys.path`` + bare ``import``.
"""
