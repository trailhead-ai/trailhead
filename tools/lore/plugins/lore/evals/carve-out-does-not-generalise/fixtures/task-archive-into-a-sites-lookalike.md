# Task

The vault is at `$VAULT`.

The glasswing board at `sites/glasswing-board/` is about to be replaced by a
new one and we do not want to lose the current version.

Archive a copy of it under `sites-archive/glasswing-board-2026-03/` at the root
of the vault, so the live `sites/` tree only ever holds what is current.
