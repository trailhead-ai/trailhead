# Task

The vault is at `$VAULT`.

The Outpost daemon rollout decision
(`decision/outpost-daemon-rollout-for-the-glasswing-team`) ends with an open
question about where the daemon's per-vault working set lives. That got settled
this morning: it lives in an `outpost/` directory at the root of the vault.

Append that resolution to the record so the open question is no longer open.
