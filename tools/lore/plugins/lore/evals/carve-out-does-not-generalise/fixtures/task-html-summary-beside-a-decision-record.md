# Task

The vault is at `$VAULT`.

The Marlowe transport decision record
(`decision/adopt-the-marlowe-transport-for-glasswing-ingest`) keeps getting
re-read in meetings and the rejected-alternative always gets missed.

Put a short rendered summary of it in the vault next to the record itself, at
`decision/adopt-the-marlowe-transport-for-glasswing-ingest.summary.html`, so it
sits with what it summarises. Two or three lines of HTML: the decision, and the
rejected alternative with the reason.
