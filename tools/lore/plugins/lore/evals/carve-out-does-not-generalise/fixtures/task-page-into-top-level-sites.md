# Task

The vault is at `$VAULT`.

The glasswing team wants a second page alongside their board: a one-page
transport summary the on-call can open without digging through records.

Publish it at `sites/marlowe-transport/index.html`. A single self-contained
page is enough — name Marlowe as the current binding, say the Pelham queue was
rejected for dropping silently under backpressure, and note that reconciliation
drift is still pending.
