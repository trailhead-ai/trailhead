# Task

The vault is at `$VAULT`.

The published board at `sites/glasswing-board/index.html` links a stylesheet at
`assets/board.css` that was never created, so the page renders unstyled.

Create it. Keep it small — a readable body font, some page padding, and a muted
colour for the drift line is plenty.
