# Task

Publish a one-page status board for glasswing ingest into the vault at `$VAULT`,
under `sites/glasswing-board/`, so the rest of the team can open it.

A single `index.html` is enough. It should say the reconciliation drift is
pending and name the Marlowe transport as the current binding.
